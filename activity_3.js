// Task 1: Arithmetic Calculator

// Step 1: Prompt the user to enter two numbers
let num1 = "Enter the first number:";
let num2 = "Enter the second number:";

// Step 2: Perform arithmetic operations
let sum = num1 + num2;
let difference = num1 - num2;
let product = num1 * num2;
let quotient = num1 / num2;

// Step 3: Display the results
console.log("Sum: " + sum);
console.log("Difference: " + difference);
console.log("Product: " + product);
console.log("Quotient: " + quotient);

// Task 2: Age Validator

// Step 1:  the user to enter their age
let age = "Enter your age:";

// Step 2: Check if the user is 18 or older
let isAdult = age >= 18;

// Step 3: Conditional statement to display an appropriate message
if (isAdult) {
    console.log("You are an adult.");
} else {
    console.log("You are not an adult.");
}
